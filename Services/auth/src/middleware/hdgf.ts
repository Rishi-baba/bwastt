import { Request, Response, RequestHandler } from "express";

const TRYCTACH = (handler: RequestHandler): RequestHandler=>{
  return async (req:Request, res:Response)=>{
    try {
      
    } catch (error) {
      
    }
  }
}